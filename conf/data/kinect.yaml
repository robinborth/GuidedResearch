# @package data
_target_: lib.data.datamodule.DPHMDataModule

# dataset settings
dataset_name: dphm_christoph_mouthmove
data_dir: ${paths.data_dir}/${data.dataset_name}
start_frame_idx: 0 
image_scale: 0.1
image_width: 1920
image_height: 1080

# training settings
batch_size: 1
num_workers: 0
pin_memory: False
drop_last: False
persistent_workers: False
shuffle: False

# dataset settings
dataset:
  _target_: lib.data.dataset.DPHMPointDataset
  _partial_: True
  data_dir: ${data.data_dir} 
  optimize_frames: ${model.optimize_frames}
  start_frame_idx: ${data.start_frame_idx}
  # this is done dynamically in coars to fine strategy
  image_scale: ???
  image_width: ???
  image_heigt: ???
