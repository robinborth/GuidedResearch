# @package data
_target_: lib.data.datamodule.KinectDataModule

# dataset settings
dataset_name: dphm_christoph_mouthmove
data_dir: ${paths.data_dir}/${data.dataset_name}
sequence_length: 120

# camera settings
width: 1920
height: 1080
near: 0.01
far: 100
scale: 8

# training settings
batch_size: 1
num_workers: 0
pin_memory: False
persistent_workers: False 

# dataset settings
train_dataset:
  _target_: lib.data.dataset.DPHMParamsDataset
  _partial_: True
  # start_frame: 52
  # end_frame: 84 
  start_frame: 14
  end_frame: 110
  jump_size: 1
  data_dir: ${data.data_dir}
  camera: ???  # the camera is shared

# dataset settings
val_dataset:
  _target_: lib.data.dataset.DPHMParamsDataset
  _partial_: True
  start_frame: 110
  end_frame: 120
  jump_size: 1
  data_dir: ${data.data_dir}
  camera: ???  # the camera is shared

renderer: ???