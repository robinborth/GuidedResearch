# @package data
_target_: lib.data.datamodule.optimize_latent.LatentOptimizationDataModule
# paths
dataset_name: dphm_christoph_mouthmove
data_dir: ${paths.data_dir}/${data.dataset_name}
# training
batch_size: 1
num_workers: 7
pin_memory: True
persistent_workers: True
drop_last: False
shuffle: False
# dataset 
dataset:
  _target_: ??? 
  _partial_: True
  data_dir: ${data.data_dir} 
