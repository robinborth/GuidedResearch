# @package _global_

defaults:
  - data: kinect
  - model: flame 
  - trainer:
    - base
    - joint 
    - sequential
  - logger: wandb
  - hydra: default
  - paths: default
  - debug: null
  - optional local: default
  - _self_

seed: 123
task_name: optimize
tags: ["optimize"]
device: cuda