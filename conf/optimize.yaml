# @package _global_

defaults:
  - model: flame 
  - logger: wandb
  - data: kinect
  - loss: point2plane 
  - optimizer: levenberg_marquardt
  - trainer:
    - joint_debug
    - sequential_debug
    # - joint
    # - sequential
  - hydra: default
  - paths: default
  - _self_

seed: 123
device: cuda
task_name: optimize
tags: 
  - ${task_name}