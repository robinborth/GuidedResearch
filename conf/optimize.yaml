# @package _global_

defaults:
  - data: dphm_flame  # dphm_points, dphm_flame
  - model: flame_points
  - trainer: coarse_to_fine  # default, coarse_to_fine
  - logger: wandb # default, wandb
  - callbacks:
    - rich_progress_bar
  - hydra: default
  - paths: default
  - debug: null
  - optional local: default
  - _self_

seed: 123
task_name: optimize
tags: ["optimize"]