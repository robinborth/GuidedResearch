# @package _global_

defaults:
  - data: kinect # kinect, flame
  - model: flame 
  - trainer: icp  # default, gpu
  - logger: wandb # csv, wandb, tensorboard
  # - callbacks:
    # - rich_progress_bar
  - scheduler:  # this is internally handled like a callback
    - coarse2fine
    - optimizer 
    # - optimizer 
  - hydra: default
  - paths: default
  - debug: null
  - optional local: default
  - _self_

seed: 123
task_name: optimize
tags: ["optimize"]