# @package _global_

defaults:
  - data: flame  # kinect, flame
  - model: point2point # point2point, point2plane
  - trainer: coarse2fine  # default, coarse2fine
  - logger: csv # csv, wandb
  - callbacks:
    - rich_progress_bar
  - hydra: default
  - paths: default
  - debug: null
  - optional local: default
  - _self_

seed: 123
task_name: optimize
tags: ["optimize"]