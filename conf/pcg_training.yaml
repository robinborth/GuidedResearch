# @package _global_

defaults:
  - data: pcg
  - model: pcg 
  - callbacks: default
  - logger: default
  - trainer: lightning 
  - hydra: default
  - paths: default
  - optional local: default
  - _self_

seed: 123
device: cuda
task_name: pcg_training
tags: 
  - ${task_name}
