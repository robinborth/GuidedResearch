def ternary_search(
    obj_func,  # function that evaluates the loss and the gradient
    x,  # the init parameters
    t,  # the step size
    d,  # the update
    f,  # the init loss value
    c1=1e-4,
    c2=0.9,
    tolerance_change=1e-9,
    max_ls=25,
):
    pass
